fn print {
    loop {
        // duplicate the pointer to current char
        dup 
        // load byte from memory
        load_byte 
        dup 0 = if {
            drop drop
            break
        }
        putc
        1 +
    }
}

// takes two elements from the sequence
fn fibonacci {
    loop {
        over
        over
        +
        rot
        dup 100 < if {
            break
        }
        putu
        ", " print
    }
}

fn main {
    1 1 fibonacci
}
